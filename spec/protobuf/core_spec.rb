require 'spec_helper'

describe Protobuf::Core do
  it 'has a version number' do
    expect(Protobuf::Core::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
